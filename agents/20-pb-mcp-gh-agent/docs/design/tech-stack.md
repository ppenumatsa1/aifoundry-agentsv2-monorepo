# Tech Stack

- Python 3.11
- Azure AI Projects SDK (`AIProjectClient`)
- Azure OpenAI Responses API (conversation + response flow)
- Azure Identity (`DefaultAzureCredential`)
- Microsoft ARM REST call via `requests` (connection creation path)
- Pydantic settings (`pydantic-settings`)
- pytest (unit + integration tests)
- OpenTelemetry + Azure Monitor exporter (optional App Insights)
