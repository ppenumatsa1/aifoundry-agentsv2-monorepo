name: aifoundry-agentsv2-demo
metadata:
  template: aifoundry-agentsv2-demo
infra:
  provider: bicep
  path: infra
  parameters:
    userPrincipalId: ${USER_PRINCIPAL_ID}
hooks:
  preprovision:
    shell: sh
    run: sh ./scripts/set-user-principal-id.sh
